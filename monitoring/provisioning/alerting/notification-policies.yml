apiVersion: 1

# Minimal default policy using the built-in contact point.
policies:
  - orgId: 1
    receiver: bookride-webhook
    group_by: ["alertname"]
    group_wait: 30s
    group_interval: 5m
    repeat_interval: 4h

# Reference: example extra route (commented out)
#  - orgId: 1
#    receiver: Discord
#    group_by: ["alertname"]
#    group_wait: 30s
#    group_interval: 5m
#    repeat_interval: 4h
