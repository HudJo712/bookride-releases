apiVersion: 1

contactPoints:
  - orgId: 1
    name: bookride-webhook
    receivers:
      - uid: bookride-webhook
        type: webhook
        settings:
          url: ${GRAFANA_WEBHOOK_URL}
          httpMethod: POST
        disableResolveMessage: false
