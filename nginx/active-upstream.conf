# Routes all traffic to BLUE and adds a header so you can see it
upstream app_backend { server api_blue:8080; }
map "" $release_color { default "blue"; }
server {
  listen 127.0.0.1:9; # dummy to keep this file valid if included alone
}
# temporary tweak
